## Freehand coding Blog

So the code should include:

State variables — probably an address public owner and maybe a uint public balance or similar
A function that's supposed to be the constructor but has a typo in its name — so it stays a regular callable function instead of running automatically
A withdraw function restricted to owner only

##date 9/16/2026
testcode1.sol 
charity1-3.sol

